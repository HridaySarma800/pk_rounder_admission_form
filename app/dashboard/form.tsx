'use client'

import { useState } from 'react'
import { Widget } from '@typeform/embed-react'
import { PopupButton } from '@typeform/embed-react'

export default function Form() {

    return (
        <PopupButton id="luOUVLqN" className="my-button">
            open form
        </PopupButton>
    );
}
